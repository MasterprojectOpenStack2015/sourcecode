#!/bin/bash

log -vv -vvv "Controller is online again, note that this *doesn't* mean that OpenStack is fully functional again. Cluster synchronization may take a while and the controller may be damaged. You may want to run \"restore_snapshot.sh initial\"."